# Clawdboard Configuration

# Gateway API URL (required for live data)
VITE_GATEWAY_URL=http://localhost:3033

# Gateway API Token (required for authentication)
VITE_GATEWAY_TOKEN=your-gateway-token-here

# Optional: Enable mock data fallback when Gateway is unavailable
VITE_USE_MOCK_FALLBACK=true
